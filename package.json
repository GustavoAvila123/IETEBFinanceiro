{
  "name": "ieteb-financeiro",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de gestão financeira IETEB — entradas, saídas, dashboard, monitor.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
